general {
	interval = 10
}

order += "disk /home"
order += "load"
order += "memory"
order += "time"

disk "/home" {
	format = "/home %avail"
}

load {
	format = "%1min"
}

memory {
	format = "RAM %used"
}

time {
	format = "%Y-%m-%d %H:%M"
}
