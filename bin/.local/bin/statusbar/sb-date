#!/bin/sh

case "$BLOCK_BUTTON" in
    1) notify-send "$(cal)" ;;
    6) setsid -f "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

value=$(date '+%b %-d %Y %-I:%M %p')
icon=""

printf '%s %s\n' "$icon" "$value"
