#!/bin/sh

get_local_ip() {
    if ! local_ip=$(ip route get 1); then
        printf 'Offline\n'
        return
    fi

    local_ip=${local_ip#* via * dev * src }
    local_ip=${local_ip% uid *}

    printf '%s\n' "$local_ip"
}

value=$(get_local_ip)
icon=" "

printf '%s %s\n' "$icon" "$value"
