#!/bin/sh

set -eu

BASE_URL="https://uguu.se"

die() {
    echo "${0##*/}: $1" >&2
    exit 1
}

file="$1"

[ ! -f "${file}" ] && die "no such file"

curl \
    --silent \
    --fail \
    --form "files[]=@${file}" \
    "${BASE_URL}/upload?output=text"
