#!/bin/sh

INTERVAL=3

while :; do
    read -r one five fifteen _ </proc/loadavg

    printf '%s' "\
one|float|$one
five|float|$five
fifteen|float|$fifteen
"

    printf '\n'
    sleep "$INTERVAL"
done
